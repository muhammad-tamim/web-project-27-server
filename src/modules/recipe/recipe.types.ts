export interface RecipeInterface {
    imageURL: string,
    title: string,
    ingredients: string,
    instructions: string,
    cuisineType: string[],
    preparationTime: number,
    categories: string[],
    likeCount: number
}

